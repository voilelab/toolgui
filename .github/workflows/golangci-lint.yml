name: GolangCI-Lint

on:
  push:
    branches: [ "dev" ]
    paths-ignore: [ "docs/**" ]
  pull_request:
    branches: [ "dev" ]
    paths-ignore: [ "docs/**" ]

jobs:
  golangci-lint:
    name: GolangCI-Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          # Pin to go.mod: a Go newer than golangci-lint's own build panics it
          go-version-file: go.mod
          cache-dependency-path: go.sum
      
      - name: Fake build assets
        run: |
          cd toolgui-web
          mkdir -p app/build/static
          echo "fake" > app/build/index.html
          echo "fake" > app/build/static/fake.txt

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.13
